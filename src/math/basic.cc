#include "basic.h"

//using namespace TOOLS::MATH;

/*template <typename T>
TOOLS::MATH::mean<T>::mean(const T& box) : container(box) { }

//template <typename T>
//mean<T>::mean() : container(T()){}
  
template<typename T>
//mean<T>::operator typename T::value_type() {
TOOLS::MATH::mean<T>::operator mean<T>::tValue() {
    tIter it = container.begin();
    tValue num = tValue(0);
    tValue out = *it;
    ++it;

    for(; it!=container.end(); ++it, ++num)
      out += *it;
    return out / num;
}*/

